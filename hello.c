// Sum of Two Numbers in C Program
#include <stdio.h>

int main() {
    int p, q,,a, r;
    
    printf("Enter two integer values::\n\n");
    scanf("%d %d", &p, &q);

    // calculating sum
    r = p + q +a ;      
    //print
    printf("Result:: %d + %d+%d = %d\n", p, q,a, r);
    return 0;
}
